import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

import Link from 'next/link';

// ... (previous imports)

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-[#F5F5F7]`}
      >
        {/* Global Header - Apple Style Glass */}
        <div className="fixed top-0 left-0 right-0 z-50 bg-[#F5F5F7]/80 backdrop-blur-md border-b border-[#0000000d] transition-all duration-300">
          <div className="max-w-[1600px] mx-auto px-6 lg:px-12 h-[48px] flex items-center justify-between">
            <Link href="/" className="flex items-center gap-2 group">
              <div className="w-5 h-5 bg-[#1d1d1f] rounded text-white flex items-center justify-center text-[10px] font-bold">N</div>
              <span className="font-semibold text-[13px] tracking-wide text-[#1d1d1f] group-hover:text-[#06c] transition-colors">NoiseReduction</span>
            </Link>

            <div className="flex gap-8 text-[12px] font-medium text-[#1d1d1f]/80">
              <Link href="/" className="hover:text-[#06c] transition-colors">Curated</Link>
              <Link href="/courses" className="hover:text-[#06c] transition-colors text-purple-600 font-semibold">Courses 🆕</Link>
              <Link href="/favorites" className="hover:text-[#06c] transition-colors">Favorites</Link>
            </div>
          </div>
        </div>

        <div className="pt-[48px]">
          {children}
        </div>
      </body>
    </html>
  );
}
