# 图片字幕生成器 PRD

## 1. 产品概述
- 名称：图片字幕生成器
- 简述：在用户上传的图片底部叠加多行字幕，每一行文字背后都有统一样式的背景条，条与条之间形成清晰分隔的“切割感”，支持预览与保存成新图片。

## 2. 目标与价值
- 快速为图片添加底部多行字幕，满足社交分享、文章插图、宣传素材等场景。
- 保证字幕可读性与统一视觉风格，强调每行之间的分隔感。

## 3. 用户与场景
- 目标用户：内容创作者、运营人员、设计师、开发者。
- 主要场景：制作用于社交媒体、博客、公众号的配图；为教程/海报添加底部分行字幕。

## 4. 核心功能清单
- 图片选择：通过文件选择器上传本地图片并显示路径与预览。
- 样式参数：
  - 字幕高度（px）：控制每一行背景条的高度。
  - 字体大小（px）：控制每行文字字号。
  - 字体颜色：文字填充颜色。
  - 轮廓颜色：文字描边颜色。
- 文本输入：多行文本输入框，按换行分行。
- 生成预览：将设置应用于图片并在页面中展示生成结果。
- 保存图片：下载生成后的图片为 PNG 文件。

## 5. 详细需求说明
### 5.1 文本与背景布局规则
- 位置：字幕区域固定在图片底部，水平居中对齐。
- 行分割：将输入框中的内容按换行符拆分为 N 行；空行也占位（绘制背景条但文字为空）。
- 背景条（切割感）：
  - 每一行文字均绘制一条独立的横向背景条，条高一致、样式一致（颜色、透明度、圆角/无圆角保持统一）。
  - 条与条之间保持固定间隙，形成清晰的分隔与“切割感”。
  - 所有背景条宽度一致，左右留有统一内边距，整体与图片底部对齐。
- 文字样式：
  - 先描边（`轮廓颜色`），后填充（`字体颜色`），提高对比度与可读性。
  - 水平居中排版；上下居中对齐到各自背景条。
- 安全边距：字幕整体区域与图片底部边缘预留不小于 20px 的安全边距；左右亦预留不小于 20px。
- 行数与高度计算：
  - 每行背景条高度 = `字幕高度（px）`。
  - 总高度 = N × 行高 + (N - 1) × 行间隙。
  - 若总高度超过图片高度的 40%，提示用户调整参数或行数。

### 5.2 颜色与透明度
- 文字填充色：由 `字体颜色` 控制。
- 文字描边色：由 `轮廓颜色` 控制，描边宽度默认 2px。
- 背景条颜色：统一使用半透明深色（默认 `rgba(0, 0, 0, 0.5)`）；V1 固定值，不提供可视化参数；后续版本可考虑开放配置。

### 5.3 输入与生成
- 文本输入：支持手动输入与粘贴；不做自动换行，超长文本由用户通过回车进行分行。
- 生成预览：点击“生成字幕图片”后在同页显示合成结果。
- 保存图片：点击“保存图片”后下载 PNG，尺寸与原图一致。

### 5.4 交互与错误处理
- 未选择图片时点击生成/保存：给出明显提示“请先选择图片”。
- 参数校验：`字幕高度`、`字体大小`必须为正数且不超过图片高度的合理范围。
- 文本为空：允许生成，仅绘制背景条形成切割感（可用于留空效果）。
- 文件类型：仅接受常见图片格式（png/jpg/jpeg/webp）。

## 6. 页面与组件
- 页面结构：
  - 标题：`图片字幕生成器`。
  - 文件选择器：显示选中文件路径。
  - 参数区：`字幕高度(px)`、`字体大小(px)`、`字体颜色`、`轮廓颜色` 输入控件。
  - 文本区：多行文本输入框。
  - 操作区：按钮“生成字幕图片”“保存图片”。
  - 预览区：显示合成结果图片。

## 7. 技术方案（前端）
- 基础：HTML + CSS + JavaScript。
- 核心：Canvas 2D。
- 绘制流程：
  1) 载入原图到 Canvas；
  2) 根据行数计算总字幕区域高度与起始位置；
  3) 逐行绘制背景条（统一样式、固定间隙）；
  4) 在各条上居中绘制文字：`strokeText` 后 `fillText`；
  5) 生成预览（`canvas` 转 `dataURL`）并展示；
  6) 保存（`canvas.toBlob` → 触发下载）。

## 8. 默认参数（V1.1）
- 字幕高度：40px。
- 字体大小：20px。
- 字体颜色：#FFFFFF。
- 轮廓颜色：#000000。
- 背景颜色：#000000（可配置）。
- 背景透明度：0.5（0-1，可配置）。
- 背景圆角：6px（可配置）。
- 行间隙：8px（可配置）。
- 左右内边距：24px（可配置）。
- 底部边距：20px（可配置）。
- 字体：系统默认中文字体优先（如 `Microsoft YaHei`），回退到通用无衬线。

## 9. 兼容与性能
- 兼容：现代桌面浏览器（Chrome/Edge/Firefox/Safari）最新两个大版本。
- 性能：单张图片（≤10MB）合成时间 ≤ 1s（在常见设备上）。

## 10. 验收标准
- 选择图片、设置参数、输入多行文本后，点击生成可得到预览，底部每行文字背后均有统一样式背景条，条间分隔清晰，呈现“切割感”。
- 点击保存后，下载到本地 PNG，效果与预览一致、尺寸与原图一致。
- 参数与交互符合本 PRD，错误提示清晰。

## 11. 里程碑（V1）
- 完成页面布局与交互。
- 实现 Canvas 合成（统一背景条、描边+填充文字、分隔切割感）。
- 支持预览与下载。

## 12. V1.1 扩展
- 新增参数面板：背景颜色、背景透明度、背景圆角、行间隙、左右内边距、底部边距。
- 绘制策略：背景条支持圆角，颜色基于`rgba(背景颜色, 背景透明度)`统一应用于所有行。
- 验收补充：
  - 变更后的参数对预览与导出效果一致生效。
  - 将行间隙设为0时，各背景条紧密排列但仍保持独立；当透明度降低或圆角变化时视觉一致性仍符合“统一样式”与“切割感”。

## 13. V1.2 扩展
- 移动端适配：在宽度≤900px时参数面板两列、≤560px时单列显示；控件可触达且不溢出。
- 字体与字重：新增字体选择（如 `Microsoft YaHei`、`Noto Sans SC`、`PingFang SC`、`Arial`、`Helvetica`）与字重选择（400/500/600/700）。
- 字间距：支持以像素为单位的字间距；居中排版时以“字符宽度总和+字间距”计算整体宽度并居中。
- 验收补充：
  - 字体与字重变化能在预览与导出保持一致；字间距在不同行、不同文本长度下居中准确。
  - 移动端上滑动与输入体验流畅，按钮易点击，预览区自适应屏幕宽度。

## 14. V1.3 扩展
- 国际化：新增`语言`选择（中文/英文），为页面标题、表单标签、按钮文案、文本框占位统一切换；提示语同样切换。
- 批量文本导入：支持导入`.txt/.csv/.json`文本文件并自动填充多行字幕：
  - `txt`：按换行拆分为多行。
  - `csv`：按行读取，每行的列以空格拼接为一行文本。
  - `json`：解析为字符串数组，每个元素一行。
- 验收补充：
  - 语言切换后所有文案即时更新，生成与保存行为不受影响；错误提示随语言变化。
  - 文本导入后可正常预览与导出；导入无效文件时保持当前文本不变并给予提示（后续可增强）。
